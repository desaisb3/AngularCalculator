# Calculator and Shopping Cart Web Application using Angular and .net

To run it on your machine:
-  Clone the repo.
-  run `npm install`
-  start serving the project on the browser using `ng serve -o` 

